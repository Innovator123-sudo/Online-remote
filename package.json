{
  "name": "cast-remote",
  "version": "2.0.0",
  "description": "Phone-to-Chromecast gesture remote. No PC bridge, no dependencies.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
